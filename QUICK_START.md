# 🚀 5분 만에 시작하기

## 1단계: 설치 (1분)

```bash
# 프로젝트로 이동
cd /Users/john/projects/ai-task-manager

# 의존성 설치
npm install

# localtunnel 설치
npm install -g localtunnel

# .env 파일 설정
cp .env.example .env
# .env 파일 열어서 GEMINI_API_KEY 입력
```

## 2단계: 실행 (30초)

### 터미널 1️⃣ - 서버
```bash
npm start
```
✅ `Server running on http://localhost:3000` 확인

### 터미널 2️⃣ - 터널
```bash
./start-tunnel.sh
```
✅ `your url is: https://gpt-task-api.loca.lt` 확인

## 3단계: 브라우저 테스트 (30초)

브라우저에서 열기:
```
https://gpt-task-api.loca.lt
```

1. "Click to Continue" 클릭
2. 메인 페이지 보이면 성공! ✅

## 4단계: ChatGPT 설정 (2분)

### 1. GPT Builder 열기
```
ChatGPT → Explore → Create a GPT
```

### 2. Configure 탭 클릭

### 3. Actions 클릭 → Create new action

### 4. 이 코드 복사-붙여넣기:

```json
{
  "openapi": "3.1.0",
  "info": {
    "title": "Task API",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://gpt-task-api.loca.lt"
    }
  ],
  "paths": {
    "/task": {
      "post": {
        "operationId": "createTask",
        "summary": "지시사항 전송",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "instructions": {
                    "type": "string"
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Success"
          }
        }
      }
    }
  }
}
```

### 5. Instructions (선택사항)
```
사용자가 개발을 요청하면 상세한 요구사항을 수집한 후 createTask를 호출하세요.

형식:
목표: [...]
핵심기능:
- 기능1
- 기능2
기술스택: [...]
구현요구사항:
1. ...
2. ...
```

### 6. Save 클릭

## 5단계: 테스트! (1분)

### ChatGPT에서 시도:
```
"간단한 계산기 웹앱을 만들어줘"
```

### 확인:
1. ChatGPT가 요구사항 질문함
2. 답변 후 티켓 생성됨
3. 터미널에서 로그 확인:
   ```
   📥 새로운 지시사항 수신
   🎫 Jira 티켓 생성: TASK-1
   ```

### 대시보드 확인:
```
http://localhost:3000/dashboard.html
```

### 개발 시작:
1. 티켓의 **[🚀 개발]** 버튼 클릭
2. 잠시 대기 (Gemini가 코드 생성 중)
3. `projects/` 폴더 확인!

## ✅ 완료!

이제 다음처럼 사용하세요:

```
ChatGPT에게 말하기
    ↓
티켓 자동 생성
    ↓
대시보드에서 [개발] 클릭
    ↓
코드 자동 생성!
```

## 🔥 팁

### 터미널 레이아웃
```
┌─────────────────┬─────────────────┐
│  터미널 1       │  터미널 2       │
│                 │                 │
│  npm start      │  ./start-      │
│                 │  tunnel.sh      │
│                 │                 │
│  [서버 로그]    │  [터널 로그]    │
└─────────────────┴─────────────────┘
```

### 자주 쓰는 명령어
```bash
# 서버 재시작
Ctrl+C → npm start

# 터널 재시작
Ctrl+C → ./start-tunnel.sh

# 로그 확인
tail -f logs/server-out.log
```

### 문제 발생 시
1. 터미널에 에러 로그 확인
2. `http://localhost:3000` 접속 테스트
3. `https://gpt-task-api.loca.lt` 접속 테스트
4. 둘 다 안되면 재시작

## 📚 더 알아보기

- [상세 가이드](docs/LOCALTUNNEL_GUIDE.md)
- [README.md](README.md)
- [GitHub](https://github.com/chosundeveloper/ai-task-manager)
